import { DmAbilityModifierPipe } from './dm-ability-modifier.pipe';

describe('DmAbilityModifierPipe', () => {
  it('create an instance', () => {
    const pipe = new DmAbilityModifierPipe();
    expect(pipe).toBeTruthy();
  });
});
