import { DmSkillModifierPipe } from './dm-skill-modifier.pipe';

describe('DmSkillModifierPipe', () => {
  it('create an instance', () => {
    const pipe = new DmSkillModifierPipe();
    expect(pipe).toBeTruthy();
  });
});
