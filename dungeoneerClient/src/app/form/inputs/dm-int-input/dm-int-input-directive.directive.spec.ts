import { DmIntInputDirectiveDirective } from './dm-int-input-directive.directive';

describe('DmIntInputDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new DmIntInputDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
